{
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "start": "yarn workspace app start",
    "env:pull": "vercel env pull app/.env",
    "prepare": "husky install",
    "app:lint:format": "yarn workspace app lint:fix && yarn workspace app format"
  },
  "dependencies": {
    "@playwright/test": "^1.16.1",
    "@types/cytoscape": "^3.14.15",
    "cytoscape-dagre": "^2.3.2",
    "cytoscape-svg": "^0.3.1",
    "playwright": "^1.16.1"
  },
  "devDependencies": {
    "husky": "^6.0.0"
  },
  "engines": {
    "node": "^16"
  }
}
